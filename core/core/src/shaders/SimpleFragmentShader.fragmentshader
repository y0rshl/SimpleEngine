#version 330 core

uniform vec4 outColor;

uniform sampler2D u_texture;
in vec2 UV;


// Ouput data
out vec4 color;

void main()
{

	// Output color = red
	//Sample uvcoord
	vec2 uvcoord = vec2(300,300);
	color = texture(u_texture,UV);

}