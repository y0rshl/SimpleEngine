#version 330 core

out vec4 color;
void main()
{
    vec3 convertedShadowCoordenate = (gl_FragCoord.xyz + vec3( 1, 1, 1)) / 2.0;
    float depthBuffer = convertedShadowCoordenate.z;
    color = vec4(depthBuffer, depthBuffer, depthBuffer, 1);
}